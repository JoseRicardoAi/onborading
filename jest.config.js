// @ts-check

/** @type {import('jest').Config} */
const config = {
  modulePathIgnorePatterns: ['<rootDir>/apps/web/.next/'],
  testPathIgnorePatterns: ['<rootDir>/apps/web/.next/'],
};

module.exports = config;
